export enum Roles {
    User = 1,
    Api = 2,
    Admin = 3
}

// @TODO: czy to jest potrzebne? może wystarczy sam enum?

export const ROLES = {
    "User": Roles.User,
    "Api": Roles.Api,
    "Admin": Roles.Admin,
}
